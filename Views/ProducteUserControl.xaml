<UserControl x:Class="Projecte2.Views.ProducteUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projecte2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="90">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Source="{Binding PrimeraFoto}" Width="80" Height="80" Margin="5" Grid.Row="0"/>
        <TextBlock Text="{Binding nom}" FontSize="16" HorizontalAlignment="Center"
            Margin="10" Grid.Row="1"/>
    </Grid>
</UserControl>
